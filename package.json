{
  "name": "devops-backend",
  "version": "1.0.0",
  "description": "DevOps CI/CD demo backend service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "lint": "eslint src/backend/ tests/ --ext .js",
    "test": "jest --coverage --forceExit",
    "test:ci": "jest --coverage --forceExit --ci"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testMatch": ["**/tests/**/*.test.js", "**/?(*.)+(spec|test).js"],
    "collectCoverageFrom": [
      "src/backend/**/*.js",
      "!src/backend/server.js"
    ],
    "coverageDirectory": "coverage",
    "coverageThreshold": {
      "global": {
        "branches": 80,
        "functions": 80,
        "lines": 80,
        "statements": 80
      }
    }
  }
}
